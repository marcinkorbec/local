import filters from './filters.action';
import tableResults from './tableResults.action';
import currentPage from './currentPage.action';
import pageTitle from './pageTitle.action';

const allActions = { filters, tableResults, currentPage, pageTitle };

export default allActions;
